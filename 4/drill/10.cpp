#include "../../std_lib_fac.h"
int main() 
{
	char user_choice { '?' };
	vector<char> choices { 's', 'p', 's', 'r', 's', 'r', 'r', 'r', 'r', 'p' };
	for(int i = 0; i < choices.size(); ++i) {
		cout << "Введите камень, ножницу или бумагу(r/s/p)\n";
		cin >> user_choice;
		switch
	}
}
